# Everyone builds Raccoon with the same toolchain: stable, plus the two
# components the four checks need. `rustup` installs this on first build.
[toolchain]
channel = "stable"
components = ["clippy", "rustfmt"]
targets = ["aarch64-apple-darwin"]
